{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "uri": "${process.env.DATABASE}",
        "database": "${process.env.DATABASE_NAME}"
      },
      "options": { "ssl": true }
    }
  }
}
